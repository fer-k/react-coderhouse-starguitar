import React from 'react';

const ProductDetail = ({product}) => {

    return (
        <div>
            <p>{product.title}</p>
        </div>
    )       
            
}
    
export default ProductDetail